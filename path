// pega todos os arquivos
// checa quais sao owners, actions, paths

-> pega todos os arquivos de owners
-> usa a funcao getLogOwners passando todos os owners (merge)
-> cria os owners

-> cria as rules de owners
-> cria as rules de actions (usando os owners)
-> cria as rules de path


como pegar o path de um log

const getLogPath = buildGetLogPath({}, logOwners);
